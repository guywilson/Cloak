#include <stdint.h>

#ifndef _INCL_TYPES
#define	_INCL_TYPES

typedef uint8_t				byte;
typedef uint16_t			word;
typedef uint32_t			dword;

typedef byte *				PBYTE;
typedef char *				PSZ;

#endif
